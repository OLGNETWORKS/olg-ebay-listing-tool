# T2 — Base de aplicación, contratos y compilador estático

**Rama:** `task/T2-compiler` · **Worktree operativo:** `C:/Users/T14/.codex/worktrees/0648/computerUSE` · **Base:** `integration/mvp-foundation@8fce736e9ef5def307f8eda27150b2be88a43664`

El ledger reserva `C:/Users/T14/olg-listing-tool-flota/w-T2/repo`; este corte se ejecuta en el worktree aislado administrado por Codex indicado arriba. No se comparte carpeta ni se trabaja sobre la rama de integración.

Trabajas junto a otras IAs. No reviertas cambios ajenos y adapta tu trabajo a lo ya integrado.

## Objetivo

Dejar una base técnica consumible por T3–T5: configuración de Next.js App Router, React y TypeScript estricto; PostgreSQL como objetivo de persistencia sin crear todavía modelos ni migraciones; contratos de dominio independientes del framework; y un primer compilador determinista de `DescriptionDocument` JSON a HTML/CSS estático compatible con eBay, construido con TDD.

## Dependencias y contratos congelados

- T1 está integrada en `integration/mvp-foundation@8fce736e9ef5def307f8eda27150b2be88a43664`.
- `ListingProfile` separa identidad, datos comerciales, logística, media y `descriptionDocument`.
- `DescriptionDocument` contiene `schemaVersion`, `theme` y `blocks[]`; el JSON es la fuente editable.
- `Compiler.compile(document)` devuelve `{ html, css, warnings, errors }` sin React ni JavaScript en la salida.
- `ListingGateway.validateDraft(profile)` y `ListingGateway.publishDraft(profile, approval)` son contratos de dominio; no contienen adaptadores ni tráfico de red.
- `publishDraft` sólo será invocable en tareas posteriores a través de una capa de aplicación con autorización y confirmación humana.
- T3 es dueño de `prisma/**`; T4 es dueño de `src/app/**`; T5 es dueño de `src/lib/ebay/**`. T2 no adelanta esas zonas.

## Evidencia confirmada e inferencias

- Confirmado: el plan congela JSON por bloques, compilación estática, gateway mock inicial y producción bloqueada.
- Confirmado: T2 es el único dueño inicial de dependencias/configuración compartida y desbloquea T3–T5.
- Inferencia técnica: Prisma se prepara como la capa futura de acceso a PostgreSQL porque T3 posee `prisma/**`; T2 instala la base compartida pero no conecta una base de datos ni crea credenciales.

## Tu ownership — nada fuera

```text
package.json
package-lock.json
tsconfig.json
next.config.ts
postcss.config.mjs
src/lib/contracts/**
src/lib/compiler/**
tests/compiler/**
```

Excepción explícita de coordinación: este brief `orchestration/briefs/T2.md` se crea antes de implementar porque T2 no tenía brief y la solicitud delegada lo exige.

## Prohibido

- Modificar cualquier archivo fuera del ownership y de la excepción del brief.
- Crear `src/app/**`, `prisma/**`, adaptadores de eBay o jobs.
- Escribir en eBay real, Sandbox o producción; no se permite tráfico a eBay.
- Usar credenciales, tokens, claves o secretos en fixtures, Git o logs.
- Generar JavaScript, formularios, iframes, recursos HTTP o comportamiento sticky en el HTML del anuncio.
- Mezclar la rama propia o la rama de integración, publicar, desplegar o hacer push.

## TDD y verificación

1. Añadir una prueba mínima y ejecutarla para observar el fallo correcto.
2. Implementar lo mínimo para pasarla.
3. Repetir RED → GREEN por comportamiento y refactorizar sólo en verde.
4. Ejecutar todos los checks completos de T2 y los checks heredados de T1.

```powershell
npm run test:compiler
npm test
npm run typecheck
npm run check:framework
node --test tests/orchestration/*.test.mjs
node scripts/orchestration/validate-ledger.mjs
```

## Reporte

`GREEN|RED|BLOCKED`; SHA; salida de checks; `git diff --stat`; desviaciones. Un solo commit: `[T2] base de aplicación, contratos y compilador estático`.
