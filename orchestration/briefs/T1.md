# T1 — Fundación de coordinación multi-IA

**Rama:** `task/T1-foundation` · **Worktree:** `C:/Users/T14/olg-listing-tool-flota/w-T1/repo` · **Base:** `integration/mvp-foundation@c487268`

Trabajas junto a otras IAs. No reviertas cambios ajenos y adapta tu trabajo a lo ya integrado.

## Objetivo

Cerrar la fundación operativa de coordinación con un ledger validable y un tablero Markdown generado de forma determinista: el validador comprueba estructura, referencias, DAG, ownership y worktrees; el renderer produce un dashboard y una tarjeta por tarea con estados expresados mediante símbolo y palabra.

## Dependencias y contratos congelados

- Node.js 20 o posterior, sin dependencias externas.
- `orchestration/ledger.json` es la única fuente editable del estado de coordinación.
- `orchestration/tablero/_dashboard.md` y `orchestration/tablero/T<n>.md` son artefactos generados; no se editan manualmente.
- `blocked_by_human` es un estado válido y distinto de un fallo técnico.
- La rama base permanece en `integration/mvp-foundation@c487268`; esta tarea no mezcla ramas.

## Tu ownership — nada fuera

```text
orchestration/**
scripts/orchestration/**
tests/orchestration/**
```

## Prohibido

- Modificar cualquier archivo fuera del ownership, incluidos `AGENTS.md`, `CLAUDE.md`, `README.md`, `docs/**`, `FROOITION_RADIOGRAFIA_Y_PRODUCTO.md` y `.gitignore`.
- Escribir en eBay real.
- Usar credenciales o secretos en fixtures o logs.
- Mezclar la rama propia o la rama de integración.

## TDD y verificación

1. Añade una prueba mínima y ejecútala para observar el fallo correcto.
2. Implementa lo mínimo para pasarla.
3. Ejecuta los checks completos de esta tarea.

```powershell
node scripts/orchestration/validate-ledger.mjs
node --test tests/orchestration/validate-ledger.test.mjs tests/orchestration/render-dashboard.test.mjs
node scripts/orchestration/render-dashboard.mjs
git diff --check
git diff --stat
git status --short
```

El validador acepta opcionalmente una ruta JSON y el renderer acepta opcionalmente la ruta JSON y el directorio de salida:

```powershell
node scripts/orchestration/validate-ledger.mjs [ruta-al-ledger.json]
node scripts/orchestration/render-dashboard.mjs [ruta-al-ledger.json] [directorio-de-salida]
```

## Reporte

`GREEN|RED|BLOCKED`; SHA; comandos y resultados; `git diff --stat`; desviaciones. Un solo commit: `[T1] Fundación de coordinación multi-IA`.
